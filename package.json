{
  "name": "electroneumjs",
  "version": "1.0.0",
  "description": "A NodeJS package to manage transactions in Electroneum",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "watch": "babel src --out-dir dist --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SyncroIT",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.17.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-3": "^6.24.1"
  }
}
